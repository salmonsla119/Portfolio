# 포트폴리오 편집 방법

## 가장 먼저 열 파일

- 화면 확인: `index.html`
- 대부분의 문구와 목록 수정: `assets/data.js`
- 자기소개서 본문 수정: `assets/data.js`의 `coverLetter` 항목

메모장보다 Visual Studio Code로 폴더 전체를 열어 수정하는 것을 권장합니다. 저장한 뒤 브라우저에서 새로고침하면 결과를 확인할 수 있습니다.

## 이미지 넣기

1. 넣을 이미지 파일을 `assets/images` 폴더에 복사합니다. 폴더가 없으면 새로 만듭니다.
2. 파일명은 영문과 숫자로 작성합니다. 예: `project-01-main.jpg`
3. `assets/data.js`에서 해당 이미지 경로를 `assets/images/project-01-main.jpg`처럼 입력합니다.

프로필 사진은 새 사진의 파일명을 `profile.jpg`로 바꾼 뒤 `assets/profile.jpg`를 교체하면 됩니다.

## 메인 페이지 수정

`assets/data.js`에서 다음 항목을 찾습니다.

- `metrics`: 참여 프로젝트, 리더 역할, 경력 수치
- `specialties`: 특기 사항 네 칸 (아이콘·제목·설명이 표시됩니다. `eyebrow`의 영문 라벨은 더 이상 화면에 나오지 않습니다)
- `games`: 플레이한 게임 목록
- `projects`: 포트폴리오 프로젝트 목록

메인의 대표 프로젝트는 `projects` 배열에서 가장 첫 번째 프로젝트입니다. `image`에 대표 이미지 경로를 입력합니다.

이미지 경로에는 폴더 사이의 `/`를 반드시 넣습니다. 올바른 예시는 `assets/images/project-01-main.png`입니다. `assetsimagesproject-01-main.png`처럼 입력하면 파일을 찾을 수 없습니다. 포트폴리오 목록은 `image`를 우선 사용하고, `image`가 비어 있으면 `images`의 첫 번째 이미지를 썸네일로 사용합니다.

## 게임 목록 추가

`games` 안의 항목 하나를 복사해서 쉼표로 구분하여 붙여 넣습니다.

```js
{id:"game-13",title:"게임명",platform:"PC",genre:"RPG",record:"120시간 · 엔딩 클리어"}
```

`genre`에 입력한 장르를 기준으로 장르 필터, 우측 순위, 육각형 그래프가 자동 생성됩니다. 같은 장르의 게임이 많을수록 높은 순서로 표시되며 육각형에는 상위 6개가 들어갑니다.

## 이력서 인적사항 입력

`assets/data.js`의 `resumePersonalInfo`를 수정합니다. `label`에는 항목명, `value`에는 내용을 입력합니다. 객체를 복사해 항목을 늘릴 수 있습니다.

```js
resumePersonalInfo:[
  {year:"연도",history:"이력"},
  {year:"연도",history:"이력"}
]
```

## 이력서 프로젝트 목록 (옆으로 흐르는 목록)

`resumeProjects`에 넣은 항목이 이력서에서 **옆으로 천천히 흐르는 목록**으로 표시됩니다. 마우스를 올리면 멈추고, 좌우로 끌거나 오른쪽 위 ← → 버튼으로 넘길 수 있습니다. 항목을 복사해 붙여 넣으면 개수가 늘어납니다.

## 이력서 경력과 프로젝트 입력

`assets/data.js`의 `resumeCareers`와 `resumeProjects`를 수정합니다. 빈 항목을 채우거나 객체를 복사하여 개수를 늘릴 수 있습니다.

```js
resumeCareers:[
  {period:"기간",company:"회사명",role:"직무",detail:"업무 내용"}
],
resumeProjects:[
  {year:"연도",title:"프로젝트명",role:"역할",detail:"설명"}
]
```

원본 이력서 PDF는 새 파일을 `kim-chaeyeon-resume.pdf`로 바꾼 뒤 `assets/kim-chaeyeon-resume.pdf`를 교체합니다.

## 보유 기술 수정

`skills`에서 다음 값을 수정합니다.

- `image`: 아이콘 파일 경로
- `name`: 프로그램 이름
- `level`: 0~100 값을 넣으면 **5개 동그라미** 중 몇 개가 칠해질지 자동으로 정해집니다 (0~10:0개, 11~30:1개, 31~50:2개, 51~70:3개, 71~90:4개, 91~100:5개)
- `detail`: 커서를 올렸을 때 표시되는 설명

## 포트폴리오 프로젝트 입력

`projects`의 각 항목을 수정합니다.

```js
{
  id:"project-01",
  featured:true,
  year:"2026",
  title:"프로젝트명",
  genre:"장르",
  role:"내 역할",
  summary:"목록과 상단에 표시할 짧은 설명",
  body:"상세 설명",
  image:"assets/images/project-01-main.jpg",
  team:[{part:"기획",count:2},{part:"아트",count:3},{part:"프로그래밍",count:2}],
  shortVideos:[{src:"assets/videos/project-01-short.mp4"}],
  images:[{src:"assets/images/project-01-01.jpg",alt:"이미지 설명",caption:"화면 설명"}],
  documents:[{title:"시스템 기획서",src:"assets/documents/system-plan.pdf"}]
}
```

포트폴리오 목록에서 크게 강조할 프로젝트에는 `featured:true`를 넣습니다. 일반 크기로 되돌리려면 `featured:false`로 바꾸거나 해당 줄을 삭제합니다.

강조 카드는 작은 카드의 **가로 2칸 × 세로 2칸**을 정확히 차지하도록 되어 있어, 크기만 커지고 비율은 작은 카드와 동일합니다. 현재 프로젝트는 12개이며 PC 화면에서 3열 × 5줄로 배치됩니다. 항목을 복사해 붙여 넣으면 줄이 늘어나고, 지우면 줄이 줄어듭니다(3의 배수로 맞추면 빈칸 없이 떨어집니다).

영상은 `assets/videos`, 기획서는 `assets/documents` 폴더를 만들어 넣으면 관리하기 쉽습니다. 배열 안에 항목을 쉼표로 구분해 여러 개 넣으면 영상·이미지·기획서가 모두 여러 개 표시됩니다.

프로젝트 상세 페이지는 **내 역할·참여 인원 → 상세 설명 → 숏폼 영상 → 프로젝트 이미지 → 상세 기획서** 순으로 표시됩니다.

- **내 역할과 참여 인원**은 제목 바로 아래에 한 줄짜리 얇은 띠로 나란히 들어갑니다.
- **상세 설명** 상자는 `summary`(간단 설명)가 큰 글씨로 먼저 나오고 그 아래 `body`(상세 설명)가 이어집니다.
- **숏폼 영상**은 가로 전체 폭으로 크게 놓입니다.
- **프로젝트 이미지**는 큰 사진 한 장과 오른쪽 목록으로 표시됩니다. 목록에서 고르면 큰 사진이 바뀌고, 목록이 길어지면 아래로 스크롤됩니다. 큰 사진은 가로 폭에 맞춰 들어가므로 세로로 긴 사진을 넣으면 그만큼 길어집니다. `caption`은 큰 사진 아래에 표시됩니다. **이미지를 아직 넣지 않아도 상자와 목록 틀은 그대로 보입니다.**
- **상세 기획서**는 제목 버튼이 상자 위에 줄지어 놓이고, 누른 문서의 제목이 상자 안의 제목이 됩니다.

상세 기획서는 `documents`에 넣은 **제목이 위쪽에 버튼으로 줄지어 뜨고, 버튼을 누르면 아래에 그 문서가 바로 펼쳐집니다.** 파일 확장자를 보고 `.png` `.jpg` 같은 이미지는 그림으로, `.pdf`는 문서 뷰어로 표시합니다. 문서 아래에는 `새 창으로 보기`와 `다운로드` 버튼이 함께 나옵니다. 항목을 여러 개 넣으면 버튼도 그만큼 늘어납니다.

### 유튜브 영상 넣기

`shortVideos`의 `src`에 유튜브 주소를 그대로 붙여 넣으면 됩니다. 영상 파일을 직접 넣어도 되고 섞어 써도 됩니다.

```js
shortVideos:[
  {src:"https://www.youtube.com/watch?v=영상아이디", title:"영상 이름"},
  {src:"assets/videos/my-short.mp4"}
]
```

가로/세로 비율은 자동으로 정해집니다. 유튜브 일반 영상은 가로(16:9), 유튜브 쇼츠와 직접 넣은 영상 파일은 세로(9:16)입니다. **바꾸고 싶으면 그 항목에 `ratio:"9/16"` 또는 `ratio:"16/9"`를 적어 주세요.**

유튜브 영상은 **미리보기 그림이 먼저 뜨고, 재생 버튼을 누르면 그 자리에서 재생**됩니다.

다만 **파일을 더블클릭해서 열면(주소가 `file://`) 유튜브가 재생을 막는 경우가 있습니다**(`오류 153`). 유튜브 정책이라 페이지 쪽에서는 풀 수 없습니다. 이때는 아래 그림 밑에 나오는 `유튜브에서 보기` 링크로 열면 됩니다. **페이지 안에서 확실히 재생되게 하려면 둘 중 하나를 쓰세요.**

1. GitHub Pages 같은 곳에 올려 `https` 주소로 연다 (가장 간단하고, 남에게 보여줄 때도 이 방법을 씁니다)
2. 영상 파일을 직접 `assets/videos` 에 넣고 `{src:"assets/videos/영상.mp4"}` 로 적는다 (인터넷 없이도 재생됩니다)

### 엑셀 파일 넣기

브라우저는 파일을 더블클릭해 열었을 때 엑셀(`.xlsx`) 내용을 직접 읽지 못합니다. 그래서 **표 내용을 미리 뽑아 둔 `assets/sheets.js`를 만들어 두고 그것을 표로 그립니다.** 원본 엑셀 파일은 다운로드용으로 그대로 씁니다.

1. 엑셀 파일을 `assets/documents/` 안에 넣습니다(파일 이름은 영문·숫자를 권장).
2. 아래 명령을 한 번 실행합니다. 파이썬만 있으면 되고 따로 설치할 것은 없습니다.

```bash
python tools/xlsx-to-sheets.py assets/documents/파일이름.xlsx 키이름
```

3. `data.js`의 `documents`에 아래처럼 적습니다. `sheet`에는 2번에서 쓴 키이름을 넣습니다.

```js
documents:[
  {title:"2026년 8월 작업 캘린더", src:"assets/documents/work-calendar-2026-08.xlsx", sheet:"work-calendar-2026-08"}
]
```

엑셀을 고친 뒤에는 2번 명령을 다시 실행해야 화면에도 반영됩니다. 명령을 실행하지 않고 `sheet` 없이 등록하면 표가 뜨는 대신 다운로드 버튼만 나옵니다.

## 자기소개서 수정

`cover-letter.html`을 직접 열어 수정할 필요 없이 `assets/data.js` 맨 위쪽의 `coverLetter` 항목만 마크다운 문법으로 작성하면 화면에 자동으로 서식이 적용되어 들어갑니다. (다른 페이지들과 마찬가지로 인터넷이나 별도 서버 없이 `cover-letter.html`을 그냥 더블클릭해서 열어도 바로 정상적으로 표시됩니다.)

```js
coverLetter:`## 지원동기

여기에 문단을 입력합니다. **굵게** 강조도 가능합니다.

## 경험 / 역량

- 목록 항목도 사용할 수 있습니다.
- 두 번째 항목`,
```

- `## 제목`처럼 쓰면 소제목(지원동기, 경험/역량 등)이 됩니다.
- 빈 줄로 문단을 구분하면 각각 하나의 문단으로 표시됩니다.
- `**굵게**`, `*기울임*`, `` `코드` ``, `- 목록`, `1. 번호 목록`, `> 인용문`, `---` 구분선, `[링크](주소)`도 사용할 수 있습니다.
- 맨 앞과 맨 뒤의 백틱(`` ` ``) 기호는 지우지 않고 그 사이의 내용만 수정합니다.

## 주의 사항

- 따옴표와 쉼표를 지우면 화면이 나오지 않을 수 있습니다.
- 이미지 배열은 `{src:"경로", alt:"설명"}`처럼 객체를 `}`로 닫은 뒤 배열을 `]`로 닫습니다. 괄호 하나가 빠지면 `data.js` 전체가 실행되지 않습니다.
- 파일 경로의 영문 대소문자를 실제 파일명과 동일하게 작성합니다.
- 수정 전 폴더를 복사해 백업하는 것을 권장합니다.


## 도트 아이콘 수정

달·구름·반짝이·보석·게임패드 아이콘은 외부 이미지 파일이 아니라 HTML 안에 직접 들어 있는 **도트(픽셀) SVG**입니다. `<rect>` 하나가 도트 한 칸이며, 색은 `assets/style.css`의 클래스에서 지정합니다.

- `.px-moon` 달, `.px-cloud` 구름, `.px-star` 별·보석, `.px-ink` 진한 남색, `.px-peach` 밝은 살구색
- 크기는 `.hero-moon`(히어로 달), `.metrics-sparkle`(카드 옆 반짝이), `.head-icon`(제목 옆 아이콘)의 `width` 값으로 조절합니다.
- 도트를 추가하려면 `<rect x="가로칸" y="세로칸" width="칸수" height="칸수"/>` 형식으로 넣습니다. 좌표는 픽셀이 아니라 **도트 칸 단위**입니다.

## 색상 변경

`assets/style.css` 맨 위 `:root` 한 곳만 고치면 모든 페이지 색이 함께 바뀝니다.

- `--paper` 배경(크림), `--ink` 글자·강조(남색), `--peach` 상단 띠·강조 카드, `--sand` 제목 밑줄·카드 그림자, `--gold` 영문 라벨·별, `--muted` 본문 갈색, `--card` 카드 흰색

## 배경·탭 디자인 조정

- **도트 별 배경**: `assets/style.css`의 `body{background-image:...}`. 별을 옅게 하려면 `background-size` 값을 키우고(별 간격이 넓어짐), 촘촘하게 하려면 줄입니다.
- **픽셀 도시 실루엣**: `.city-strip`의 `height` 값으로 높이를 조절합니다. 메인은 히어로 안에, 다른 페이지는 푸터 위 `.city-band` 안에 들어 있습니다. 특정 페이지에서 빼려면 해당 HTML의 `<div class="city-band">...</div>` 줄을 지웁니다.
- **파일철 탭**: 상단 메뉴는 현재 페이지가 탭처럼 튀어나오는 형태입니다. 각 HTML의 `<nav class="nav">` 안에서 현재 페이지 링크에만 `class="active"`를 주면 됩니다.
- **남색 외곽선**: 카드류의 `border:2px solid var(--ink)`와 `box-shadow:5px 6px 0 var(--sand)`로 되어 있습니다. 두께나 그림자 거리를 바꾸면 전체 카드가 함께 바뀝니다.
